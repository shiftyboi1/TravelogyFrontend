export type CustomSwitchProps = {
  values: {
    label: string;
    value?: string;
  }[]
  bgColor?: {
    light: string;
    dark: string;
  }
  selectionColor?: {
    light: string;
    dark: string;
  }
}

